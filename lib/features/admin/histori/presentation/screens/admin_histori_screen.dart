import 'package:flutter/material.dart';

class AdminHistoriScreen extends StatelessWidget {
  const AdminHistoriScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Histori Peminjaman')),
    body: const Center(child: Text('Admin Histori — Coming Soon')),
  );
}
