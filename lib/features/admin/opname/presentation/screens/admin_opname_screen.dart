import 'package:flutter/material.dart';

class AdminOpnameScreen extends StatelessWidget {
  const AdminOpnameScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Stock Opname')),
    body: const Center(child: Text('Admin Stock Opname — Coming Soon')),
  );
}
