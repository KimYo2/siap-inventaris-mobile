import 'package:flutter/material.dart';

class AdminTiketScreen extends StatelessWidget {
  const AdminTiketScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Tiket Kerusakan')),
    body: const Center(child: Text('Admin Tiket — Coming Soon')),
  );
}
