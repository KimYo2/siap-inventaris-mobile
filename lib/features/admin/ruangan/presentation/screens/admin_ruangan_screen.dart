import 'package:flutter/material.dart';

class AdminRuanganScreen extends StatelessWidget {
  const AdminRuanganScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('Ruangan')),
    body: const Center(child: Text('Admin Ruangan — Coming Soon')),
  );
}
